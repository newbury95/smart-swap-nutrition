
import { useMealManagement as useMealManagementRefactored } from "./meal-management";

// Re-export the refactored hook to maintain backward compatibility
export const useMealManagement = useMealManagementRefactored;
